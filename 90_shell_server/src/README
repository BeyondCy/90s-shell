mainthread: shell
2nd th: listen
3nd-nth thread: process;

avaiable cmd:
ls

switch #N

exit(exit command will never sendto client).


bugs:
when client host crash, the server can't dectect the crash.
未处理ctrl+c 信号。
未检查加入的客户端是否重复。